import { SecretType } from '../../models/SecretType';
export declare class SimpleTransactionRequest {
    walletId: string;
    to: string;
    alias?: string;
    value: number;
    secretType: SecretType;
    tokenAddress?: string;
    comments?: string;
    constructor(walletId: string, to: string, value: number, secretType: SecretType);
}
