export declare class Signer {
    static createSigner(bearerTokenProvider: () => string): Signer;
    static signer: Signer;
    private static openWindow;
    private bearerTokenProvider;
    private popup;
    private isPopupMounted;
    private eventListeners;
    private popupMountedListener?;
    private messagePort?;
    private onPopupMountedQueue;
    private constructor();
    executeTransaction(transactionRequest: any): Promise<{}>;
    signTransaction(transactionRequest: any): Promise<{}>;
    close(): void;
    addEventListener(event: string, handleFunction: () => void): void;
    private handleTransactionInPopup;
    private attachMessageHandler;
    private sendTransactionRequest;
    private cleanupPopup;
    private createPopupMountedListener;
    private processPopupMountedQueue;
    private createMessageHandler;
}
